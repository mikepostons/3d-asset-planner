# 17 September 2026 — Visible donut handles and X-ray

The radius controls were filtered out in Vertices selection. Circular diameter handles now remain visible/pickable in every Select mode, with top/bottom inner/outer labels for donuts. Move and Rotate keep their own controls. Handle materials render above translucent surfaces.

Added an editor-only X-ray toggle beside Terrain and Floor guides. Component surfaces become translucent with clearer edges. Reference capture temporarily restores solid rendering and then restores X-ray. Scene geometry and saved dimensions do not change.

Verification: 32 automated tests pass, including four-handle availability across selection modes; production build passes with the existing bundle warning. Isolated browser check confirmed labelled donut controls in Vertices mode and X-ray rendering. Export images were not manually compared.
