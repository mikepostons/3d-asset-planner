# Compact floor guide labels

Floor labels now use compact text such as F2 · 6 m, measured content width,
roughly half the previous height, a lighter background and an anchor offset to
one side of the floor-guide corner. Axis labels are unchanged. Floor guides still
control visibility and labels remain limited to the selected component.

Validation: 68 tests pass; production build passes with existing bundle warning.
