# Arches and keystones — 2026-09-18

Added segmented heads for arched doors/windows and complete circular surrounds.
Arches/Keystones toggles use component defaults and opening overrides. Sizes adds
surround width and keystone extra size. Keystones replace crown sectors and project
half their extra size further than the shared projection. Shared gaps/chamfers and
surround materials are honoured. Jambs already terminate at the arch spring.

Geometry follows the actual faceted cut contour so inner edges do not obstruct it.
Fixed segmentation is 16 sectors for arches or 32 for circles; enabling a keystone
combines two sectors. Arch stone counts and individual stone editing are not exposed.

52 tests pass, including all three curved opening kinds, exactly one centred
keystone, finite geometry, ray checks inside the clear curve, disable and scale.
Production build passes with the existing bundle warning. In isolated ?test=1,
drew an arched window and visually inspected its automatically generated surround.
No production scene data changed.
